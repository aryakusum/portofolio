# Architecture

> Auto-generated by /map on 2026-02-23

## Overview

A high-performance, immersive 3D creative portfolio built with React and Vite. The application features a dual-theme system ("Editorial" and "Retro/8-bit") and utilizes Three.js for interactive background elements.

```
┌─────────────────────────────────────────┐
│              [main.jsx]                 │
│         Entry Point (Vite)              │
├─────────────────────────────────────────┤
│               [App.jsx]                 │
│         Theme Management (Provider)     │
├───────────────┬─────────────────────────┤
│ [EditorialContent] │   [RetroContent]   │
│ Elegant Design     │   8-bit Design     │
├───────────────┴─────────────────────────┤
│            [Common Components]          │
│   Scene3D, SoundToggle, ThemeToggle     │
├─────────────────────────────────────────┤
│               [Data Layer]              │
│      src/data.js (Static Data)          │
└─────────────────────────────────────────┘
```

## Components

### Theme System

- **Purpose:** Manages global look and feel.
- **Location:** `src/context/ThemeContext.jsx`
- **Dependencies:** React Context, Framer Motion for transitions.

### 3D Scene

- **Purpose:** Interactive background elements.
- **Location:** `src/components/Scene3D/`
- **Dependencies:** `@react-three/fiber`, `three`, `gsap`.

### Editorial View

- **Purpose:** Modern, typography-focused layout.
- **Location:** `src/components/editorial/`
- **Sub-components:** `EditorialHero`, `EditorialContent`.

### Retro View

- **Purpose:** Pixel-art, 8-bit aesthetic layout.
- **Location:** `src/components/retro/`

## Data Flow

1. **Initialization**: `main.jsx` renders `App.jsx`.
2. **State Management**: `ThemeContext` provides the current theme (editorial vs retro).
3. **Content Resolution**: `App.jsx` switches visibility between `EditorialContent` and `RetroContent` based on theme.
4. **Data Injection**: Content components import and map through data from `src/data.js` (projects, skills).
5. **Animation**: `framer-motion` and `gsap` are used for entry animations and interactive transitions.

## Integration Points

| Service      | Type       | Purpose                                           |
| ------------ | ---------- | ------------------------------------------------- |
| Firebase     | Auth/DB    | Placeholder for Chat/Contact (Currently disabled) |
| GitHub Pages | Deployment | Configured via `gh-pages`                         |

## Technical Debt

- [ ] **Large Component**: `EditorialContent.jsx` (~480 lines) contains many sub-components that could be extracted.
- [ ] **Infrastructure**: Firebase is currently a dummy/stub implementation.
- [ ] **Redundancy**: `src/App.backup.jsx` should be moved to a separate backup location or removed.
- [ ] **Testing**: No automated tests (Vitest/Jest) are currently implemented for core logic.

## Conventions

**Naming:** PascalCase for components, camelCase for hooks and utilities.
**Structure:** Component-based directory structure in `src/components/`.
**Testing:** N/A (Missing).
